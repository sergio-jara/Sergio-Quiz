# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'dynamox' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for dynamox

end

target 'dynamoxTests' do
  inherit! :search_paths
  # Pods for testing
end

target 'dynamoxUITests' do
  inherit! :search_paths
  # Pods for UI testing
end
